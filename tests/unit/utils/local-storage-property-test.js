import localStorageProperty from '../../../utils/local-storage-property';
import { module, test } from 'qunit';

module('Unit | Utility | local storage property');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = localStorageProperty();
  assert.ok(result);
});
